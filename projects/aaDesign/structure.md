<!-- aaDesign

aaDesign/
├── app/                # Main application package
│   ├── __init__.py     # Initialize the Flask app, configure settings, and register blueprints.
│   ├── routes.py       # Define your application's routes and the views associated with them.
│   ├── templates/      # Store your HTML templates (e.g., base.html, index.html, etc.).
│   │   └── base.html   # Your base template with common HTML structure (header, footer, etc.).
│   └── static/         # Store static files like CSS, JavaScript, and images.
│       ├── css/        # Custom CSS stylesheets.
│       ├── js/         # Custom JavaScript files.
│       └── images/     # Images used in the website.
├── venv/               # Virtual environment containing dependencies.
├── Dockerfile          # Dockerfile for containerizing the application.
├── requirements.txt    # File for listing Python dependencies.
└── run.py              # Entry point to start the Flask application.

-->
