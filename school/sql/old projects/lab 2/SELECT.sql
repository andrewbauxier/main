-- Selects all records from the Engineers table
SELECT * FROM Engineers ORDER BY EID DESC;

-- Selects all records from the Faculty table
SELECT * FROM Faculty ORDER BY FID DESC;

-- Selects all records from the Classes table
SELECT * FROM Classes ORDER BY CID DESC;

-- Selects all records from the ClassEnrollments table
SELECT * FROM ClassEnrollments ORDER BY EnID DESC;
